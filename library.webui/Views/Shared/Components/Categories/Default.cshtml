@model List<Category>

<div class="list-group list-group-horizontal-md my-5">
    <a asp-controller="Book" asp-action="Index" asp-route-id="" class="list-group-item list-group-item-action @(ViewBag.SelectedCategory == null?"active":"")"> Tüm Kategoriler</a>
    @foreach (var category in Model)
    {
        <a asp-controller="Book" asp-action="Index" asp-route-id="@category.Id" class="list-group-item list-group-item-action @(ViewBag.SelectedCategory == category.Id.ToString()?"active":"")">@category.Name</a>
    }
</div>
